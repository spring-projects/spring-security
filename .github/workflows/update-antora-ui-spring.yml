name: Update Antora UI Spring

on:
  workflow_dispatch:

jobs:
  update:
    name: Build
    uses: spring-io/spring-security-release-tools/.github/workflows/update-antora-ui-spring.yml@cbdd2d9cdbefc61d4c940cc142894da8c935d674
    secrets:
      token: ${{ secrets.GH_ACTIONS_REPO_TOKEN }}
